import { FullSizeDirective } from './full-size.directive';

describe('FullSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new FullSizeDirective();
    expect(directive).toBeTruthy();
  });
});
